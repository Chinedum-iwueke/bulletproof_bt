[pytest]
testpaths = tests
norecursedirs = .git .venv data logs outputs research research_data research_db
markers =
    integration: opt-in integration tests that run end-to-end backtests
    smoke: smoke-level coverage checks
