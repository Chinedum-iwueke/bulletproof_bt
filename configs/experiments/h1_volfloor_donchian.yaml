version: 1
name: h1_volfloor_donchian
description: "Hypothesis 1: vol floor gating for trend continuation (Variant B + Donchian exit)"
fixed:
  data:
    entry_timeframe: "15m"
  execution:
    profile: tier2
  strategy:
    name: "volfloor_donchian"
    variant: "B"
    entry_lookback: 20
    exit_lookback: 10
    atr_stop_multiple: 2.0
    vol_window_days: 30
  risk:
    r_per_trade: 0.005
grid:
  strategy.vol_floor_pct: [60, 70, 80, 85]
  strategy.adx_min: [25]
run_naming:
  template: "adx{strategy.adx_min}_vol{strategy.vol_floor_pct}"
