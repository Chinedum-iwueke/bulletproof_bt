version: 1
name: h1_volfloor_emapullback
description: "Hypothesis 1: vol floor gating for trend continuation (Variant B + Donchian exit)"
fixed:
  data:
    entry_timeframe: "15m"
  execution:
    profile: tier2
  strategy:
    name: "volfloor_ema_pullback"
    variant: "A"
  risk:
    r_per_trade: 0.005
grid:
  strategy.vol_floor_pct: [60, 70, 80, 85, 90]
  strategy.adx_min: [18, 22, 25]
run_naming:
  template: "adx{strategy.adx_min}_vol{strategy.vol_floor_pct}"
