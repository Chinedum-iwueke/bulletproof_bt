# Non-runnable hypotheses

- `L2-H3` (`research/hypotheses/l2_h3.yaml`) currently has no explicit strategy reference key (`entry.strategy` missing), so it cannot be mapped to a registered runtime strategy by the validator.
